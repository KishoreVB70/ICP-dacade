type Course = record {
  id : nat64;
  title : text;
  updated_at : opt nat64;
  contact : text;
  creator_name : text;
  body : text;
  creator_address : text;
  created_at : nat64;
  category : text;
  keyword : text;
  attachment_url : text;
};
type CoursePayLoad = record {
  title : text;
  contact : text;
  creator_name : text;
  body : text;
  category : text;
  keyword : text;
  attachment_url : text;
};
type CourseUpdatePayLoad = record {
  title : opt text;
  contact : opt text;
  creator_name : opt text;
  body : opt text;
  category : opt text;
  keyword : opt text;
  attachment_url : opt text;
};
type Error = variant {
  EmptyFields : record { msg : text };
  NotFound : record { msg : text };
  UnAuthorized : record { msg : text };
};
type FilterPayLoad = record {
  creator_address : opt text;
  category : opt text;
  keyword : opt text;
};
type Result = variant { Ok : Course; Err : Error };
type Result_1 = variant { Ok : vec Course; Err : Error };
service : {
  add_course : (CoursePayLoad) -> (Result);
  delete_all_courses : () -> (Result_1);
  delete_course : (nat64) -> (Result);
  filter_courses_and : (FilterPayLoad) -> (Result_1) query;
  filter_courses_or : (FilterPayLoad) -> (Result_1) query;
  get_course : (nat64) -> (Result) query;
  update_course : (nat64, CourseUpdatePayLoad) -> (Result);
}
